export default function About() {
    return(
        <main className="p-5">
            <h1 className="text-xl font-bold">About</h1>
            <p>This is a simple chat application that uses the OpenAI GPT-3 model to generate responses to messages.</p>
        </main>
    );
}